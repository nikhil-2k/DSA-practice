#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cout << "N is : ";
    cin >> n;
    for(int i = 2; i<n; i++)
    {
    if (n % i == 0)
    cout<<"Not prime"<<endl;
    break;

    }
    else
    {
        cout<< 
    }

}