using namespace std;
#include<bits/stdc++.h>
struct Array
{
    int n;
    int size;
    int length;

};
void InsertSort(struct Array *arr,int x)
{  
    int i=arr->length-1;
    if (arr->length==arr->size)
    return;
    while (i>=0 && arr->A[i]>x)
    {
        /* code */
    }
    
}
int main()
{
    struct Array arr={{2,3,5,10,15},10,5};
}
